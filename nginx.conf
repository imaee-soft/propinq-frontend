server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Angular SPA: cualquier ruta del router debe volver a index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}

