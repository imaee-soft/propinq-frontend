export interface RealEstateResponse {
  id: string;
  username: string;
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  address: string;
  companyName: string;
  cuit: string;
  legalName: string;
  active: boolean;
  createdAt?: string;
  updatedAt?: string;
}
